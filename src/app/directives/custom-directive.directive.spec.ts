import { CustomDirectiveDirective } from './custom-directive.directive';

describe('CustomDirectiveDirective', () => {
  it('should create an instance', () => {
    // const directive = new Z();
    // expect(directive).toBeTruthy();
  });
});
